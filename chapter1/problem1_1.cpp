#include <iostream>
#include <string>
using namespace std;

size_t isUnique(string word){
	size_t word_length = sizeof(&word) / sizeof(char*);
}

int main() {
    cout << isUnique("sexxi");
    return 0;
}
